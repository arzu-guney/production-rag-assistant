# Documents directory

Place `.txt`, `.md`, or `.pdf` files here for local indexing.

The sample file `sample-rag-overview.md` is original content created for this repository.
Persisted vector database files are stored under `../vector_store/` and are gitignored.
